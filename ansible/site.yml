---
- name: Provision devops-lab host
  hosts: web
  become: true

  pre_tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      changed_when: false

  roles:
    - { role: baseline, tags: [baseline] }
